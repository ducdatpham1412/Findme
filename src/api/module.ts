import {getProfile} from 'asset/staticData';

// SOCKET
export const getAllListMessage: any = (id: number) => {};

// LOGIN SCREEN
export const apiSignUp = () => {};

export const apiGetProfile = (params?: any) => {
    return getProfile;
};

// SETTING
export const apiChangeLanguage = async () => {};
